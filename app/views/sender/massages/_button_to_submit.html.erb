<%= form_for( [:sender,@massage] ) do |f| %>
  <% [ :category_id, :address_id, :active_datetime, :validated_datetime].each do |attr| %>
    <%= f.hidden_field attr %>
  <% end %>
  <%= f.submit '依頼' %>
<% end %>