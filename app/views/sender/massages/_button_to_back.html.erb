<%= form_for( @massage,:url => [:new,:sender,:massage] ) do |f| %>
  <% [ :category_id, :address_id, :active_datetime, :validated_datetime,:latitude,:longitude,:description].each do |attr| %>
    <%= f.hidden_field attr %>
  <% end %>
  <%= f.submit '戻る' %>
<% end %>