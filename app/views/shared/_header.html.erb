<% if current_user %>
  ようこそ, <%= getFullUserName(current_user) %> |
  <%if current_user.roles.sender%>
    <%= link_to '送信者画面へ',sender_massages_url %>
  <% end %>
  <%if current_user.roles.receiver%>
    <%= link_to '受信者画面へ',receiver_massages_url %>
  <% end %>
  <%= link_to "ログアウト", :logout, :class=>"logout" %>
<% end %>